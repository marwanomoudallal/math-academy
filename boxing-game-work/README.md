# Math Boxing Battle

A self-contained Pygame boxing and mathematics game. Install dependencies with `pip install -r requirements.txt`, then run `python main.py`.

Type the answer to each math question and press Enter. A correct answer makes your boxer punch the Math Robot. A wrong answer makes the robot punch you. Reduce the robot's health to zero to win.
